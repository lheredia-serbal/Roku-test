<?xml version="1.0" encoding="utf-8" ?>

<component
    name="NewsItem"
    extends="Group"
    focusable="true"
    xsi:noNamespaceSchemaLocation="https://devtools.web.roku.com/schema/RokuSceneGraph.xsd">

    <!-- Script de lógica para el slider de noticias. -->
    <script type="text/brightscript" uri="NewsItem.brs" />
    <script type="text/brightscript" uri="pkg:/source/Utils.brs" />
    <script type="text/brightscript" uri="pkg:/source/Utils/i18nUtils.brs" />
    <script type="text/brightscript" uri="pkg:/source/Enums.brs" />

    <interface>
        <!-- Lista de items del slider (cada item puede tener title y imageURL/uri/image). -->
        <field id="items" type="array" onChange="itemsChanged" />
        <!-- Índice actual seleccionado para mostrar imagen/título y actualizar dots. -->
        <field id="currentIndex" type="integer" value="0" onChange="currentIndexChanged" />
        <!-- Título fallback si el item actual no trae title. -->
        <field id="title" type="string" value="" onChange="titleChanged" />
    </interface>

    <children>
        <!-- Poster de fondo que ocupa el 100% del ancho/alto de pantalla. -->
        <Poster
            id="backgroundImage"
            loadDisplayMode="scaleToZoom"/>

        <!-- Poster temporal para animar el ingreso del siguiente/anterior item durante el slide. -->
        <Poster
            id="incomingBackgroundImage"
            visible="false"
            loadDisplayMode="scaleToZoom"/>

        <!-- Capa oscura opcional para mejorar legibilidad del texto sobre la imagen. -->
        <Rectangle
            id="overlay"
            color="#000000"
            opacity="0.20"/>

        <!-- Contenedor rectangular (más ancho que alto) para la acción de detalle. -->
        <Group 
            id="detailActionGroup" 
            visible="false">

            <Rectangle 
                id="detailActionBackground" 
                color="#000000" 
                opacity="0.60" />

            <Label 
                id="detailActionLabel" 
                text="" 
                color="#FFFFFFFF" 
                font="font:MediumBoldSystemFont" />

            <Poster 
                id="detailActionIcon" 
                uri="pkg:/images/shared/more_info.png" />
        </Group>

        <!-- Timer de animación configurado para 20 frames * 0.04s = 0.8 segundos. -->
        <Timer id="slideTimer" repeat="true" duration="0.05" />
    </children>
</component>
